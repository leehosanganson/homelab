resources:
  - namespace.yaml
  - ../../base

configMapGenerator:
  - files:
      - values.yaml
    name: keda-values
    options:
      disableNameSuffixHash: true
      labels:
        helm.toolkit.fluxcd.io/watch: "true"
