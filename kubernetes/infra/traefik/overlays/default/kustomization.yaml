namespace: kube-system

resources:
  - ../../base
  - secrets.yaml

configMapGenerator:
  - files:
      - values.yaml
    name: traefik-values
    options:
      disableNameSuffixHash: true
      labels:
        helm.toolkit.fluxcd.io/watch: "true"
