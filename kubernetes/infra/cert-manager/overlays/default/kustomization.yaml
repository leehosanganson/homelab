namespace: cert-manager

resources:
  - ../../base
  - namespace.yaml
  - certificate.yaml

configMapGenerator:
  - files:
      - values.yaml
    name: cert-manager-values
    options:
      disableNameSuffixHash: true
