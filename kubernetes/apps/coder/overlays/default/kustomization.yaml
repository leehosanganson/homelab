namespace: coder

resources:
  - ../../base
  - secrets.yaml
  - backup-store.yaml
  - backup-schedule.yaml

patchesStrategicMerge:
  - db.yaml

configMapGenerator:
  - files:
      - values.yaml
    name: coder-values
    options:
      disableNameSuffixHash: true
