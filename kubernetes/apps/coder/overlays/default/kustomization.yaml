namespace: coder

resources:
  - ../../base
  - secrets.yaml
  - backup-store.yaml
  - backup-schedule.yaml

patches:
  - path: db.yaml

configMapGenerator:
  - files:
      - values.yaml
    name: coder-values
    options:
      disableNameSuffixHash: true
