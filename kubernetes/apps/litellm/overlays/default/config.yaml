apiVersion: v1
kind: ConfigMap
metadata:
  name: litellm-config
data:
  config.yaml: |
    litellm_settings:
      cache: True
      callbacks: ["prometheus"]
      request_timeout: 600
      cache_params:
        type: local
