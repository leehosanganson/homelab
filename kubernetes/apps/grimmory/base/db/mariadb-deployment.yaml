apiVersion: apps/v1
kind: Deployment
metadata:
  name: grimmory-mariadb
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: grimmory-mariadb
  template:
    metadata:
      labels:
        app: grimmory-mariadb
    spec:
      containers:
        - name: mariadb
          image: lscr.io/linuxserver/mariadb:11.8.8@sha256:b20cdf0ea4148ef800f9459078e93ba2388df6db786c4b9848fd20b5ebb0d3fa
          env:
            - name: PUID
              value: "1000"
            - name: PGID
              value: "1000"
            - name: TZ
              value: "Etc/UTC"
            - name: MYSQL_DATABASE
              value: "grimmory"
            - name: MYSQL_USER
              valueFrom:
                secretKeyRef:
                  name: grimmory-env
                  key: username
            - name: MYSQL_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: grimmory-env
                  key: password
            - name: MYSQL_ROOT_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: grimmory-env
                  key: root-password
          ports:
            - containerPort: 3306
              protocol: TCP
          volumeMounts:
            - name: mariadb-config
              mountPath: /config
          livenessProbe:
            exec:
              command:
                - mariadb-admin
                - ping
                - -h
                - localhost
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
          resources:
            limits:
              memory: "512Mi"
      volumes:
        - name: mariadb-config
          persistentVolumeClaim:
            claimName: grimmory-mariadb-pvc
