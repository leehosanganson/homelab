namespace: minecraft

resources:
  - ../../base

configMapGenerator:
  - files:
      - values.yaml
    name: minecraft1-values
    options:
      disableNameSuffixHash: true
      labels:
        helm.toolkit.fluxcd.io/watch: "true"
