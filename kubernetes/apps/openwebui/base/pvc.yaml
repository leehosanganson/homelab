apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: open-webui
  name: open-webui-pvc
spec:
  storageClassName: "nfs-csi-retain"
  accessModes: ["ReadWriteOnce"]
  resources:
    requests:
      storage: 2Gi
