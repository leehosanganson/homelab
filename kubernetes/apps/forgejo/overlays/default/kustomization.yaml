namespace: forgejo

resources:
  - ../../base
  - secrets.yaml
  - backup-store.yaml
  - backup-schedule.yaml

configMapGenerator:
  - files:
      - values.yaml
    name: forgejo-values
    options:
      disableNameSuffixHash: true
