apiVersion: snapshot.storage.k8s.io/v1
kind: VolumeSnapshot
metadata:
  name: test-snapshot
  namespace: tests
spec:
  source:
    persistentVolumeClaimName: test-velero-pvc
  volumeSnapshotClassName: synology-snapshotclass
