namespace: zotero

resources:
  - ../../base
  - secrets.yaml

configMapGenerator:
  - files:
      - values.yaml
    name: zotero-webdav-values
    options:
      disableNameSuffixHash: true
      labels:
        helm.toolkit.fluxcd.io/watch: "true"
