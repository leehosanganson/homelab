# ClusterIP service pointing to the opencode-1 NixOS VM (192.168.1.161).
# Traefik routes HTTP requests here for reverse proxying.
apiVersion: v1
kind: Service
metadata:
  name: opencode
spec:
  type: ClusterIP
  ports:
    - port: 4096
      targetPort: 4096
