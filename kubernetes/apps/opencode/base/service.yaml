# ExternalName service pointing to the opencode-1 NixOS VM (192.168.1.161).
# Traefik resolves this to the VM IP for reverse proxying.
apiVersion: v1
kind: Service
metadata:
  name: opencode
spec:
  type: ExternalName
  externalName: "192.168.1.161"
  ports:
    - port: 4096
      targetPort: 4096
