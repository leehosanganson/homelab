apiVersion: discovery.k8s.io/v1
kind: EndpointSlice
metadata:
  annotations:
    kubernetes.io/service-name: opencode
  labels:
    app: opencode
endpoints:
  - addresses:
      - "192.168.1.161"
    ports:
      - port: 4096
        protocol: TCP
