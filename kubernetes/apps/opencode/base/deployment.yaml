apiVersion: apps/v1
kind: Deployment
metadata:
  name: opencode
spec:
  replicas: 1
  selector:
    matchLabels:
      app: opencode
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: opencode
    spec:
      securityContext:
        fsGroup: 1000
      containers:
        - name: opencode
          image: ghcr.io/anomalyco/opencode:1.14.31@sha256:fc868f6adf9364ff99a9fee4662cddd54f23161bb0d9a1030c6a2373b20d3750
          imagePullPolicy: Always
          ports:
            - containerPort: 4096
          envFrom:
            - configMapRef:
                name: opencode-config
            - secretRef:
                name: opencode-secrets
          env:
            - name: HOME
              value: /home/opencode
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 1Gi
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            readOnlyRootFilesystem: true
            runAsNonRoot: true
            runAsUser: 1000
            runAsGroup: 1000
            seccompProfile:
              type: RuntimeDefault
          volumeMounts:
            - mountPath: /home/opencode
              name: opencode-workspace
            - mountPath: /tmp
              name: tmp-volume
      volumes:
        - name: opencode-workspace
          persistentVolumeClaim:
            claimName: opencode-pvc
        - name: tmp-volume
          emptyDir: {}
