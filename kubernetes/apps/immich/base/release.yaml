apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: immich
spec:
  interval: 1m
  chart:
    spec:
      chart: immich
      version: 0.13.2
      sourceRef:
        kind: HelmRepository
        name: immich-app
        namespace: flux-system
  valuesFrom:
    - kind: ConfigMap
      name: immich-values
