namespace: immich

resources:
  - ../../base
  - backup-schedule.yaml
  - backup-store.yaml
  - secrets.yaml

configMapGenerator:
  - files:
      - values.yaml
    name: immich-values
    options:
      disableNameSuffixHash: true
      labels:
        helm.toolkit.fluxcd.io/watch: "true"
