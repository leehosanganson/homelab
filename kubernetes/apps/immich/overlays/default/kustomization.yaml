namespace: immich

resources:
  - ../../base
  - backup-schedule.yaml
  - backup-store.yaml
  - secrets.yaml

patches:
  - path: db.yaml

configMapGenerator:
  - files:
      - values.yaml
    name: immich-values
    options:
      disableNameSuffixHash: true
