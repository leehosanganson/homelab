apiVersion: barmancloud.cnpg.io/v1
kind: ObjectStore
metadata:
  name: immich-backup-storage
spec:
  configuration:
    destinationPath: https://lhshomelabbackup.blob.core.windows.net/immich
    azureCredentials:
      storageAccount:
        name: immich-backup-env
        key: storage-account-name
      storageSasToken:
        name: immich-backup-env
        key: blob-sas
    wal:
      compression: gzip
    data:
      compression: gzip
  retentionPolicy: 7d
