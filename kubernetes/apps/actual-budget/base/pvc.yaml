apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: actual-budget-pvc
spec:
  storageClassName: nfs-csi-retain
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 512Mi
