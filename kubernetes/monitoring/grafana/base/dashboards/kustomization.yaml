resources:
  - https://github.com/dotdc/grafana-dashboards-kubernetes//?ref=master

generatorOptions:
  disableNameSuffixHash: true

configMapGenerator:
  - name: litellm-dashboard
    files:
      - litellm-dashboard.json

labels:
  - pairs:
      grafana_dashboard: "1"

commonAnnotations:
  grafana_folder: "Kubernetes"
