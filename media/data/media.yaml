apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: media-pvc
  namespace: media
spec:
  storageClassName: nfs-media-csi-retain
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Ti
