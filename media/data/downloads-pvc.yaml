apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: downloads-pvc
  namespace: media
spec:
  storageClassName: nfs-media-csi
  volumeName: downloads-pv
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 200Gi
